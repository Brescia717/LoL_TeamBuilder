<h1>Featured Builds</h1>

<ul>
  <% @builds.each do |build| %>
    <li><%= link_to build.title, build_path(build) %><br>
    <%= "  #{build.champion}" %></li>
  <% end %>
</ul>

<%= link_to "Create a Build", new_build_path, class: "button" %>
